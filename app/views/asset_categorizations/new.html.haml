- model_class = @asset_categorization.class
.row-fluid.page-header
  .span5
    %h3= t(".title", title: "#{model_class.model_name.human}: #{@asset_categorization.id}", default: t("helpers.views.edit.title", title: "#{model_class.model_name.human}: #{@asset_categorization.id}"))
  .span7
    .pull-right
      .btn-group
        = link_to asset_categorizations_path, class: "btn" do
          %i.icon-list
          %span= t(".index", default: t("helpers.links.index"))
      .btn-group
        = render "layouts/toolbar"
.row-fluid
  .span12
    = render "form"
