- model_class = @asset_transfer.class
.row-fluid.page-header
  .span5
    %h3= t(".title", title: "#{model_class.model_name.human}: #{@asset_transfer.id}", default: t("helpers.views.edit.title", title: "#{model_class.model_name.human}: #{@asset_transfer.id}"))
  .span7
    .pull-right
      .btn-group
        = link_to asset_transfers_path, class: "btn" do
          %i.icon-list
          %span= t(".index", default: t("helpers.links.index"))
      .btn-group
        = render "layouts/toolbar"
.row-fluid
  .span12
    = render "form"
